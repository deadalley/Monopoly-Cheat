#ifndef H_UTILITY
#define H_UTILITY

#include <string>

using namespace std;

class Utility {
  public:
    string name;
    int multiplier;
    int mortgage;
    int price;

    Utility(string name);
};

#endif
